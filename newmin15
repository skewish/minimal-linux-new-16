minimal linux new 15

1. pre-setup

64 bit debian 10
30 gb build partition (/dev/sda7)
swap (/dev/sda2)
bootloader on mbr

2. become su

3. install compile software (if not present)

apt install build-essential
apt install bc bison flex gawk python rsync
apt install libelf-dev libncurses-dev libssl-dev

4. make filesystem

cd /
mkdir /mnt/sda7
mkfs.ext4 /dev/sda7
mount -t ext4 /dev/sda7 /mnt/sda7

5. add directories

cd /mnt/sda7
mkdir {boot,dev,proc,sys,tmp}

6. download source code

https://www.kernel.org/ --> /mnt/sda7/tmp (busybox-1.32.0)
https://busybox.net/source.html --> /mnt/sda7/tmp (linux-4.19.138)

7. extract source code

cd /mnt/sda7/tmp
tar -x -f linux*.tar.xz
tar -x -f busybox*.tar.bz2
chown -R 0.0 /mnt/sda7

8. install kernel

cd /mnt/sda7/tmp/linux*
make clean
make distclean
make defconfig
make menuconfig (shut off modules and more)
make
make install INSTALL_PATH=/mnt/sda7/boot
make headers_install INSTALL_HDR_PATH=/mnt/sda7/usr
cd /mnt/sda7
ln -f -s boot/vmlinuz* vmlinuz

9. make initramfs

cd /mnt/sda7/boot
mkinitramfs -k -o initramfs-4.19.138 4.19.138

10. extract userspace

cd /mnt/sda7
gunzip -c boot/initramfs* > tmp/initramfs.tmp
cpio -i -d < tmp/initramfs.tmp

11. update busybox

cd /mnt/sda7/tmp/busybox*
make menuconfig (change install destination to /mnt/sda7)
make
make install

12. add etc/fstab

cat > /mnt/sda7/etc/fstab << 'done'
proc /proc proc defaults 0 0
udev /dev devtmpfs defaults 0 0
tmpfs /run tmpfs defaults 0 0
sysfs /sys sysfs defaults 0 0
/dev/sda7 / ext4 defaults 0 1
/dev/sda2 none swap sw 0 0
done

13. add etc/os-release

cat > /mnt/sda7/etc/os-release << 'done'
PRETTY_NAME="minimal linux new 15"
NAME="newmin15"
VERSION_ID="15"
ID="newmin"
HOME_URL=""
done

14. add etc/init.d/rcS

mkdir /mnt/sda7/etc/init.d
cat > /mnt/sda7/etc/init.d/rcS << 'done'
#!/bin/sh
mount -w -o remount /dev/sda7 /
rm /.ash_history
mount -a
swapon -a
done
chmod 0755 /mnt/sda7/etc/init.d/rcS

15. update grub

cd /
umount /dev/sda7
grub-install /dev/sda
update-grub

16. reboot

